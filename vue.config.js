module.exports = {
  // chainWebpack: config => {
  //   config.module.rules.delete('eslint');
  // }
  // process.env.NODE_ENV == 'production' ? { publicPath: 'https://cuopen-9a3af.web.app/' } : {};
};
